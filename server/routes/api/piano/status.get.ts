import { defineHandler } from 'nitro';
import { modelUrl } from '../../../utils/piano-model';
export default defineHandler(() => ({ configured: Boolean(modelUrl()), status: modelUrl()?'configured':'not_configured', message:modelUrl()?'Endpoint Basic Pitch dikonfigurasi, belum diverifikasi. Maksimal 60 detik; aransemen melodi, chord dan bass memakai aturan musikal, bukan model generatif.':'Service Basic Pitch belum terhubung. Kode adapter dan aransemen tersedia, tetapi transkripsi belum dapat dijalankan. Gunakan MP3/WAV maksimal 60 detik.', maxBytes:20_000_000, maxSeconds:60, formats:['mp3','wav'], transcription:'spotify-basic-pitch', arrangement:'rule-based-piano-v1' }));
